import { TransTaskPipe } from './trans-task.pipe';

describe('TransTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new TransTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
