import { UncheckedDirective } from './unchecked.directive';

describe('UncheckedDirective', () => {
  it('should create an instance', () => {
    const directive = new UncheckedDirective();
    expect(directive).toBeTruthy();
  });
});
